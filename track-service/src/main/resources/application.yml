spring:
  application:
    name: track-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        import-check:
          enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/ldmp?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true


amap:
    key: 0e89cffc12bbbe4b169db8e6a215a9fe
    direction-url: https://restapi.amap.com/v3/direction/driving
server:
  port: 8085
